akka {
  loglevel = DEBUG
}

http {
  interface = "0.0.0.0"
  port = 9000
}

db {
  driver="slick.driver.PostgresDriver$"
  default {
    driver = "org.postgresql.Driver"
    url = "jdbc:postgresql://localhost:5432/postgres?connectTimeout=0" #&characterEncoding=UTF-8"
    port = 5432
    user = "postgres"
    password = "123456"
  }
}
